---
name: compare-devices
description: "Side-by-side comparison of air quality between two devices"
arguments:
  - name: device1
    description: "First device ID (e.g., main_office)"
    required: true
  - name: device2
    description: "Second device ID (e.g., cafeteria)"
    required: true
---

Run the `compare-devices` skill with these arguments: $ARGUMENTS

Follow the instructions in `skills/compare-devices/SKILL.md` exactly.
